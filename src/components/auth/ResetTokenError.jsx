import styles from './ResetTokenError.module.css';

const ResetTokenError = () => {
    return (
        <div className={styles.resetTokenError}>
            Error
        </div>
    );
};

export default ResetTokenError;