// import getUsers from '../api/getUsers';
// import { useQuery } from '@tanstack/react-query';

const UsersTable = () => {
    // const {isLoading, data, error} = useQuery({
    //     queryKey: ['Users'],
    //     queryFn: getUsers
    // });
    
    return (
        <div>cia bus table</div>
    );
};

export default UsersTable;